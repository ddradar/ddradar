{
  "bindings": [
    {
      "type": "httpTrigger",
      "direction": "in",
      "name": "req",
      "authLevel": "anonymous",
      "methods": ["get"],
      "route": "v1/courses"
    },
    {
      "name": "documents",
      "type": "cosmosDB",
      "direction": "in",
      "databaseName": "DDRadar",
      "collectionName": "Songs",
      "sqlQuery": "SELECT c.id, c.name, c.nameIndex, c.series, ARRAY(SELECT o.playStyle, o.difficulty, o.level FROM o IN c.charts) as charts FROM c WHERE (c.nameIndex = -1 OR c.nameIndex = -2) ORDER BY c.nameIndex, c.nameKana",
      "connectionStringSetting": "COSMOS_DB_CONN_READONLY"
    },
    {
      "type": "http",
      "direction": "out",
      "name": "$return"
    }
  ],
  "scriptFile": "../dist/getCourseList/index.js"
}
